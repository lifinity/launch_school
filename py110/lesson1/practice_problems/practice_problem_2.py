# given following code, predict output w/o running
numbers = {1, 2, 3, 4, 5, 5, 4, 3}
print(len(numbers))

# len of set is 8 b/c that's how many elements are in the set
# !!...is what you'd think if you're a scrub who forgets that no
# doubles allowed in sets; num unique elements 5 == len(numbers)